{
  "outputDirectory": "dist",          // where `vite build` emits files
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "cleanUrls": true                   // optional – drops “.html” in URLs
}
